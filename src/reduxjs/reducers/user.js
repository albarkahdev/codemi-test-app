const initialState = {
  user: {
    data: {},
    token: null,
  },
};

const user = (state = initialState.user, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default user;
